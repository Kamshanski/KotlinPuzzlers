package coroutines.flow2.hints

interface collectIntsAndException {
	/**
	 * You can make variable, that holds lasts exception. And mutable lists for each flow, each of them are collectors for numbers emitted from flow
	 * Asynchronously collect all flows. Make a return Pair based on exception variable and mutable lists
	 */
}