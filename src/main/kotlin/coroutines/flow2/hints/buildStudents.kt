package coroutines.flow2.hints

interface buildStudents {
	/**
	 * [kotlinx.coroutines.flow.chunked] allows to take data by portions of fixed size.
	 * Use it to collect name, age and major of a single student.
	 *
	 * [kotlinx.coroutines.flow.chunked] позволяет получать данные порциями фиксированного размера.
	 * Используй это, чтобы собрать имя, возраст и специальность одного студента.
	 */
}