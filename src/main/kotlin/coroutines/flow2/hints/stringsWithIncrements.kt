package coroutines.flow2.hints

interface stringsWithIncrements {
	/**
	 * Consider [kotlinx.coroutines.flow.runningReduce] to summarize numbers from flow of increments.
	 * Then combine to two flow to make result flow
	 */
}