package coroutines.flow2.hints

interface efficientStringComputation {
	/**
	 * Flows typically are sequential. So you need a buffer, that collects numbers from flow ignoring delay of mapper lambda
	 * Search for a suitable function in documentation
	 */
}