package coroutines.flow1.hints

interface sumPopulationOfCountries {
	/**
	 * There is no [sum] operator for flow. But you may write your own.
	 * Consider [kotlinx.coroutines.flow.fold] as base of your function
	 */
}