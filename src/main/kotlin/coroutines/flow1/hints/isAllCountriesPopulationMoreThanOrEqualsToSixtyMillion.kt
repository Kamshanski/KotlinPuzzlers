package coroutines.flow1.hints

interface isAllCountriesPopulationMoreThanOrEqualsToSixtyMillion {
	/**
	 * There is no function [all] like for collections.
	 * You may write your own using [kotlinx.coroutines.flow.Flow.collect]. Look inside [kotlinx.coroutines.flow.count] operator to get general idea.
	 * Or use [kotlinx.coroutines.flow.fold] operator.
	 */
}