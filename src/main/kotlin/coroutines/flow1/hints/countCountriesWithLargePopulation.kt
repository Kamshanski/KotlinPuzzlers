package coroutines.flow1.hints

interface countCountriesWithLargePopulation {
	/**
	 * [kotlinx.coroutines.flow.filter] allows to select items with predicate.
	 * Operator [kotlinx.coroutines.flow.count] allows to count items from flow until flow is over.
	 */
}